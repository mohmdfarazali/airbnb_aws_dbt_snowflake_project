[project]
name = "aws-dbt-snowflake"
version = "0.1.0"
description = "AWS DBT Snowflake AirBnb Project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.12",
    "dbt-snowflake>=1.11.6",
]
